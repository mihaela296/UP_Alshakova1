<Window x:Class="UP_Alshakova.GuestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Просмотр товаров - Гость" 
        Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource PrimaryBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Шапка -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="{StaticResource SecondaryBackgroundBrush}" 
                    Height="50" VerticalAlignment="Center">
            <TextBlock Text="Просмотр товаров - Гость" Style="{StaticResource HeaderTextBlockStyle}" 
                      VerticalAlignment="Center" Margin="10,0"/>

            <!-- ФИО пользователя -->
            <TextBlock x:Name="txtUserInfoHeader" Text="Гость" Style="{StaticResource BaseTextBlockStyle}" 
                      VerticalAlignment="Center" Margin="300,0,0,0" FontWeight="Bold"/>

            <Button x:Name="btnBack" Content="Назад" Click="btnBack_Click" 
                    Style="{StaticResource AccentButtonStyle}" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Список товаров -->
        <ScrollViewer Grid.Row="1" Margin="10">
            <ItemsControl x:Name="itemsProducts">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" Margin="0,5" Padding="10"
                                Background="{Binding BackgroundColor}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>

                                <!-- Фото товара -->
                                <Image Grid.Column="0" Source="{Binding ImagePath}" Width="80" Height="80" 
                                       Stretch="Uniform"/>

                                <!-- Основная информация -->
                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="{Binding CategoryName}" FontWeight="Bold" 
                                               Style="{StaticResource BaseTextBlockStyle}"/>
                                    <TextBlock Text="{Binding ProductName}" FontWeight="Bold"
                                               Style="{StaticResource BaseTextBlockStyle}"/>
                                    <TextBlock Text="{Binding Description}" 
                                               Style="{StaticResource BaseTextBlockStyle}"/>
                                    <TextBlock Text="{Binding Manufacturer}" 
                                               Style="{StaticResource BaseTextBlockStyle}"/>
                                    <TextBlock Text="{Binding Supplier}" 
                                               Style="{StaticResource BaseTextBlockStyle}"/>
                                </StackPanel>

                                <!-- Цены и информация справа (как в макете) -->
                                <StackPanel Grid.Column="2" Margin="10,0,0,0" HorizontalAlignment="Right">
                                    <!-- Актуальная цена -->
                                    <TextBlock x:Name="txtFinalPrice" 
                                               Text="{Binding FinalPrice, StringFormat={}{0:0.00}}"
                                               Style="{StaticResource BaseTextBlockStyle}"
                                               FontWeight="Bold"/>

                                    <!-- Количество и единица измерения -->
                                    <TextBlock Text="{Binding UnitName}" 
                                               Style="{StaticResource BaseTextBlockStyle}"/>
                                    <TextBlock Text="{Binding StockQuantity, StringFormat={}{0} шт.}" 
                                               Style="{StaticResource BaseTextBlockStyle}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>